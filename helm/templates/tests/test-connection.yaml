apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vantage-node.fullname" . }}-test-connection"
  labels:
    {{- include "vantage-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vantage-node.fullname" . }}:{{ .Values.service.port }}/test']
  restartPolicy: Never
