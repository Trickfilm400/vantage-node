apiVersion: v1
kind: Service
metadata:
  name: {{ include "vantage-node.fullname" . }}
  labels:
    {{- include "vantage-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vantage-node.selectorLabels" . | nindent 4 }}
